{
    "name": "wc-grid-table-marker-input",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "build": "browserify index.js > bundle.min.js",
        "build_min": "browserify -p tinyify index.js > bundle.min.js",
        "publish": "npm run build && git add * && git commit && npm version patch && git push && npm publish && git push --tags",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "browserify": "^17.0.0",
        "browserify-css": "^0.15.0",
        "tinyify": "^4.0.0"
    }
}